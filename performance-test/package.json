{
  "name": "nestjs-ssr-performance-test",
  "version": "1.0.0",
  "description": "Performance testing suite comparing NestJS SSR with Next.js, Remix, and traditional NestJS",
  "private": true,
  "type": "module",
  "scripts": {
    "benchmark": "./scripts/run-benchmark.sh",
    "build": "./scripts/build-all.sh",
    "cleanup": "./scripts/cleanup.sh",
    "docker:up": "docker-compose -f docker/docker-compose.yml up -d",
    "docker:down": "docker-compose -f docker/docker-compose.yml down",
    "docker:logs": "docker-compose -f docker/docker-compose.yml logs -f"
  },
  "dependencies": {
    "autocannon": "^7.15.0",
    "cli-table3": "^0.6.5",
    "chalk": "^4.1.2"
  },
  "devDependencies": {
    "clinic": "^13.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
