/**
 * Dependency injection token for ErrorReporter.
 *
 * This symbol is used to inject the ErrorReporter implementation
 * throughout the application.
 */
export const ERROR_REPORTER = Symbol('ERROR_REPORTER');
